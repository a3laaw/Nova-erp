import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/card';

export default function ItemMovementReportPage() {
    return (
        <Card dir="rtl">
            <CardHeader>
                <CardTitle>تقرير حركة الصنف (بطاقة الصنف)</CardTitle>
                <CardDescription>سيتم بناء هذا التقرير لعرض تاريخ حركة صنف معين.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>قريباً...</p>
            </CardContent>
        </Card>
    );
}
